on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  release:
    types: [published]
  workflow_dispatch:

name: pkgdown

jobs:
  pkgdown:
    permissions:
      contents: write
    uses: ggsegverse/.github/.github/workflows/pkgdown.yaml@main
