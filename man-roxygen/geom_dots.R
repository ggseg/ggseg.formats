#' @param ... Captures a deprecated `sf` argument: if supplied it is converted
#'   to the polygon representation via [sf_to_polygons()] and a deprecation
#'   warning is issued. Prefer passing 2D geometry via `geom`.
