% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atlas-utils.R
\name{atlas_view_remove_region}
\alias{atlas_view_remove_region}
\title{Remove region geometry from views}
\usage{
atlas_view_remove_region(atlas, pattern, match_on = c("label", "region"))
}
\arguments{
\item{atlas}{A \code{brain_atlas} object}

\item{pattern}{Character pattern to match against region names or labels.
Uses \code{grepl(..., ignore.case = TRUE)}.}

\item{match_on}{Column to match against: "label" (default) or "region".
When "region", labels are looked up from core metadata.}
}
\value{
Modified \code{brain_atlas} with matching region geometries removed
}
\description{
Removes matching regions from the 2D sf data only. Core metadata,
palette, and 3D rendering data are unchanged. Use this for fine-grained
cleanup of specific regions in certain views, or before plotting to
hide regions without altering the atlas definition.
}
\examples{
\dontrun{
# Remove cortex outline from tract atlas views
atlas <- atlas |> atlas_view_remove_region("cortex")

# Remove specific label from 2D rendering
atlas <- atlas |> atlas_view_remove_region("lh_unknown", match_on = "label")
}
}
