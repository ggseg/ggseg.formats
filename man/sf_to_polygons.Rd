% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atlas-polygons.R
\name{sf_to_polygons}
\alias{sf_to_polygons}
\title{Convert an sf atlas geometry to the lite polygon format}
\usage{
sf_to_polygons(sf_data)
}
\arguments{
\item{sf_data}{An sf-class data.frame with columns \code{label}, \code{view},
\code{geometry} (sfc of MULTIPOLYGON).}
}
\value{
A tibble with one row per \code{label} and a \code{geometry} list-column.
Each nested element is a tibble with columns \code{view}, \code{x}, \code{y},
\code{group} (disjoint polygon piece within a label/view), \code{subgroup}
(ring within a piece; first = exterior, rest = holes).
}
\description{
Extracts coordinates from an sf-backed atlas geometry table and returns a
nested tibble keyed by \code{label}. Each row carries a \code{geometry} list-column
containing the per-view, per-ring point coordinates needed to render with
\code{\link[ggplot2:geom_polygon]{ggplot2::geom_polygon()}} (using the \code{subgroup} aesthetic for holes).
}
\examples{
\dontrun{
polys <- sf_to_polygons(dk()$data$sf)
}
}
