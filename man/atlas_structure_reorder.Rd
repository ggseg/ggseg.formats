% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atlas_structure_reorder.R
\name{atlas_structure_reorder}
\alias{atlas_structure_reorder}
\title{Reorder the structures of an atlas}
\usage{
atlas_structure_reorder(
  atlas,
  structures,
  .before = NULL,
  .after = NULL,
  match_on = c("label", "region")
)
}
\arguments{
\item{atlas}{A \code{ggseg_atlas} object.}

\item{structures}{Structures to move, as a character vector matched
against \code{match_on}. They are moved together, keeping the order given
here.}

\item{.before, .after}{One structure to move \code{structures} next to. Give at
most one of the two.}

\item{match_on}{Column of \code{core} that \code{structures}, \code{.before} and \code{.after}
name; \code{"label"} (the default) or \code{"region"}.}
}
\value{
The \code{ggseg_atlas}, with its geometry rows in the new order.
}
\description{
Moves structures within an atlas's geometry, the way \code{\link[dplyr:relocate]{dplyr::relocate()}}
moves columns. Geometry rows are drawn in the order they appear, so this
is what decides which structure is painted over which where two overlap.

Overlap is common in subcortical atlases: a slab is a thick slice of the
volume flattened into one panel, so structures that never touch in the
brain can still overlap on the page.
}
\details{
\strong{Later rows are drawn on top.} With neither \code{.before} nor \code{.after}, the
structures move to the front, matching \code{\link[dplyr:relocate]{dplyr::relocate()}}, which means
they are drawn \emph{first}, and so end up \emph{behind} everything else. To bring a
structure to the front visually, move it after the last one.

The order is a property of the structure, not of a single view: an atlas
holds one geometry row per structure with its views nested inside, so a
structure keeps the same depth in every view it appears in.
}
\examples{
a <- aseg()

# Draw the thalamus on top of everything else
last <- utils::tail(atlas_labels(a), 1)
a <- atlas_structure_reorder(a, "Left-Thalamus", .after = last)

# Or tuck it behind its neighbours
a <- atlas_structure_reorder(a, "Left-Thalamus", .before = "Left-Putamen")
}
\seealso{
\code{\link[=atlas_view_reorder]{atlas_view_reorder()}}, which moves whole views around the
canvas rather than structures within them.

Other atlas manipulations: 
\code{\link{atlas_manipulation}}
}
\concept{atlas manipulations}
