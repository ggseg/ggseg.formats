% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atlas_setters.R
\name{set_atlas_palette}
\alias{set_atlas_palette}
\title{Set the palette of an atlas}
\usage{
set_atlas_palette(atlas, value)
}
\arguments{
\item{atlas}{a \code{ggseg_atlas} object}

\item{value}{Named character vector of colours keyed by atlas \code{label}.}
}
\value{
The \code{ggseg_atlas} with its palette replaced.
}
\description{
Replaces the colour palette of a brain atlas, validating that \code{value} is a
named character vector and warning if it does not cover every atlas label.
This is the setter counterpart to the \code{\link[=atlas_palette]{atlas_palette()}} accessor; it returns
the modified atlas, so it composes with the pipe.
}
\examples{
a <- aseg()
labs <- atlas_labels(a)
a <- set_atlas_palette(a, setNames(grDevices::rainbow(length(labs)), labs))
}
\seealso{
\code{\link[=atlas_palette]{atlas_palette()}} to read the palette.

Other atlas setters: 
\code{\link{set_atlas_type}()}
}
\concept{atlas setters}
