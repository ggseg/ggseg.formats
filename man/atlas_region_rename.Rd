% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atlas-utils.R
\name{atlas_region_rename}
\alias{atlas_region_rename}
\title{Rename regions in atlas}
\usage{
atlas_region_rename(atlas, pattern, replacement)
}
\arguments{
\item{atlas}{A \code{brain_atlas} object}

\item{pattern}{Character pattern to match against region names}

\item{replacement}{Replacement string or function. If a function, it receives
the matched region names and should return new names.}
}
\value{
Modified \code{brain_atlas} with renamed regions
}
\description{
Renames regions matching a pattern to a new name, or applies a
transformation function. Only affects the \code{region} column (human-readable
names), not the \code{label} column (technical identifiers).
}
\examples{
\dontrun{
# Simple replacement
atlas <- atlas |> atlas_region_rename("bankssts", "banks of STS")

# Using a function
atlas <- atlas |> atlas_region_rename(".*", toupper)
}
}
