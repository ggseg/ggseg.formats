% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atlas_setters.R
\name{set_atlas_type}
\alias{set_atlas_type}
\title{Set the type of an atlas}
\usage{
set_atlas_type(atlas, value)
}
\arguments{
\item{atlas}{a \code{ggseg_atlas} object}

\item{value}{Atlas type; one of \code{"cortical"}, \code{"subcortical"}, \code{"tract"} or
\code{"cerebellar"}.}
}
\value{
The \code{ggseg_atlas} with its type, subclass and payload class in
agreement.
}
\description{
Replaces the type of a brain atlas. The type is held in three coupled
places: the \code{type} field, the leading \verb{<type>_atlas} class, and the
\verb{ggseg_data_<type>} class of the data payload. This setter reconstructs the
atlas through \code{\link[=ggseg_atlas]{ggseg_atlas()}} so all three stay in agreement -- assigning
the \code{type} field directly leaves the subclass stale and
\code{\link[=is_tract_atlas]{is_tract_atlas()}} and friends disagreeing with \code{\link[=atlas_type]{atlas_type()}}.
}
\details{
Because type and payload are coupled, the new type must match the data the
atlas already carries: a \code{"tract"} atlas needs \code{\link[=ggseg_data_tract]{ggseg_data_tract()}}
(centerlines), a \code{"subcortical"} atlas needs \code{\link[=ggseg_data_subcortical]{ggseg_data_subcortical()}}
(meshes). Retyping an atlas whose payload does not match is an error --
rebuild the payload with the matching \verb{ggseg_data_*()} constructor first.
}
\examples{
a <- aseg()
a <- set_atlas_type(a, "subcortical")
atlas_type(a)
}
\seealso{
\code{\link[=atlas_type]{atlas_type()}} to read the type.

Other atlas setters: 
\code{\link{set_atlas_palette}()}
}
\concept{atlas setters}
