% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atlas-utils.R
\name{atlas_region_remove}
\alias{atlas_region_remove}
\title{Remove regions from atlas}
\usage{
atlas_region_remove(atlas, pattern, match_on = c("region", "label"))
}
\arguments{
\item{atlas}{A \code{brain_atlas} object}

\item{pattern}{Character pattern to match against region names or labels.
Uses \code{grepl(..., ignore.case = TRUE)}.}

\item{match_on}{Column to match against: "region" (default) or "label"}
}
\value{
Modified \code{brain_atlas} with matching regions completely removed
}
\description{
Completely removes regions matching a pattern from the atlas. Affected
regions are removed from core, palette, sf geometry, and 3D rendering
data (vertices/meshes). Use \code{\link[=atlas_region_contextual]{atlas_region_contextual()}} instead if you
want to keep the geometry for visual context.
}
\examples{
\dontrun{
# Remove white matter from subcortical atlas
atlas <- atlas |> atlas_region_remove("White-Matter")

# Remove by label pattern
atlas <- atlas |> atlas_region_remove("^lh_", match_on = "label")
}
}
