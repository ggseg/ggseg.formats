% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atlas-utils.R
\name{atlas_region_keep}
\alias{atlas_region_keep}
\title{Keep only specified regions}
\usage{
atlas_region_keep(atlas, pattern, match_on = c("region", "label"))
}
\arguments{
\item{atlas}{A \code{brain_atlas} object}

\item{pattern}{Character pattern to match against region names or labels.
Uses \code{grepl(..., ignore.case = TRUE)}.}

\item{match_on}{Column to match against: "region" (default) or "label"}
}
\value{
Modified \code{brain_atlas} with only matching regions kept
}
\description{
Inverse of \code{atlas_region_remove()} - keeps only regions matching the pattern.
Non-matching regions are removed from core, palette, and 3D rendering data
(vertices/meshes). The 2D geometry (sf) is preserved to maintain brain
surface continuity.
}
\examples{
\dontrun{
# Keep only basal ganglia structures
atlas <- atlas |> atlas_region_keep("caudate|putamen|pallidum")

# Keep only left hemisphere
atlas <- atlas |> atlas_region_keep("^lh_", match_on = "label")
}
}
